task assemble {
    dependsOn gradle.includedBuild('xtraplatform-spatial').task(':assembleAll')
}
task bintrayUpload {
    dependsOn gradle.includedBuild('xtraplatform-spatial').task(':bintrayUploadAll')
}
