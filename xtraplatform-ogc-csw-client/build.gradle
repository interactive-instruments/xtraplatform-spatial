
version = '2.2.0'


dependencies {    
    provided project(':xtraplatform-ogc-wfs-api')

    //TODO: ???
    provided group: 'com.fasterxml', name: 'aalto-xml', version: '0.9.11'
    provided group: 'com.fasterxml.staxmate', name: 'staxmate', version: '2.2.1'
}
 
jar {
    manifest { 
        
        instructionFirst 'Import-Package', '!org.apache.xml.*'
        /*
        instruction 'Import-Package', 'javax.xml.stream.util'
        instruction 'Import-Package', 'javax.xml.stream.events'
        instruction 'Import-Package', 'org.xml.sax.helpers'
        instruction 'Import-Package', '*'
        
         */
    }
}
